cluster.name: "${cluster_name}"
xpack.security.enabled: "false"
node.master: "${is_master}"
node.data: "${is_data}"
network.host: "_site_"
http.port: "${http_transport_port}"
transport.tcp.port: "${native_transport_port}"
path.data: "/srv/elasticsearch/data"
path.logs: "/srv/elasticsearch/logs"
bootstrap.memory_lock: "true"
discovery.zen.minimum_master_nodes: "${min_master_nodes}"
discovery.zen.ping.unicast.hosts: "${master_nodes_addresses}"